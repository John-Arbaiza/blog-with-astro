<div id="hero">
	<a href="/blog" class="hero-badge">
		<span class="badge-dot"></span>
		Nuevo artículo disponible — Leer ahora →
	</a>

	<h1>Bienvenido a <span class="text-gradient">DevFundamentals</span></h1>

	<p class="subtitle">
		Artículos prácticos sobre desarrollo web, contenedores, Linux y bases de datos.
		Desde los primeros pasos hasta temas avanzados sin relleno, con ejemplos reales.
	</p>

	<div class="actions">
		<a href="/blog" class="btn btn-primary">Leer Artículos</a>
		<a href="/about" class="btn btn-secondary">Sobre el proyecto</a>
	</div>
</div>

<style>
	#hero {
		text-align: center;
		padding: 5rem 1rem 6rem;
		animation: fadeUp 0.7s ease-out forwards;
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 0;
	}

	/* — Badge — */
	.hero-badge {
		display: inline-flex;
		align-items: center;
		gap: 8px;
		font-size: 0.8rem;
		font-weight: 600;
		color: var(--text-primary);
		background: var(--bg-morado);
		border: 1px solid rgba(168, 85, 247, 0.25);
		border-radius: 999px;
		padding: 6px 14px;
		text-decoration: none;
		margin-bottom: 2rem;
		transition: all 0.25s ease;
	}

	/* Título, subtítulo y descripción */
	h1 {
		font-size: 3rem;
		font-weight: 600;
		line-height: 1.15;
		margin-bottom: 1.25rem;
		letter-spacing: -0.5px;
	}

	.text-gradient {
		background-image: linear-gradient(45deg, var(--accent-color), #818cf8);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
	}

	.subtitle {
		font-size: 1.15rem;
		color: var(--text-primary);
		max-width: 560px;
		margin: 0 auto 1.5rem;
		font-weight: 500;
		line-height: 1.7;
	}

	/* Buttons */
	.actions {
		display: flex;
		gap: 1rem;
		justify-content: center;
	}

	.btn {
		padding: 0.8rem 2rem;
		border-radius: 8px;
		font-weight: 600;
		text-decoration: none;
		transition: all 0.3s ease;
	}

	.btn-primary {
		background: var(--bg-morado);
		color: var(--text-primary);
		box-shadow: 0 4px 14px 0 rgba(56, 189, 248, 0.39);
	}

	.btn-secondary {
		color: var(--text-primary);
		border: 2px solid rgba(255, 255, 255, 0.1);
	}

	/* — Hovers con :global() para evitar problemas de scoping — */
	:global(#hero .hero-badge:hover) {
		background: rgba(168, 85, 247, 0.2);
		border-color: rgba(168, 85, 247, 0.5);
		transform: translateY(-2px);
		cursor: pointer;
	}

	:global(#hero .btn-primary:hover) {
		background: rgba(168, 85, 247, 0.2);
		cursor: pointer;
		transform: translateY(-2px);
		border-color: rgba(168, 85, 247, 0.5);
		box-shadow: none;
	}

	:global(#hero .btn-secondary:hover) {
		background: rgba(168, 85, 247, 0.2);
		border-color: rgba(168, 85, 247, 0.5);
		transform: translateY(-2px);
		cursor: pointer;
	}
</style>